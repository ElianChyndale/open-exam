import { ProfileForm } from '@/components/profile/ProfileForm';

export default function SetupPage() {
  return <ProfileForm heading="Set up your CFA workspace" detail="OpenExam uses this profile to fit reviews, practice, and mock work into your real calendar." />;
}
