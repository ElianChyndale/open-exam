import { ProfileForm } from '@/components/profile/ProfileForm';

export default function SettingsPage() {
  return <ProfileForm heading="Study settings" detail="Adjust the planning assumptions stored in your local evidence ledger." />;
}
