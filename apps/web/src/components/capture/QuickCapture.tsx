'use client';

interface Props {
  isOpen: boolean;
  onClose: () => void;
}

/** @deprecated QuickCapture modal has been replaced by the global AI Assistant drawer. */
export default function QuickCapture(_props: Props) {
  return null;
}
