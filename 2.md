系统生态审计与升级计划
1 背景与总体评估
对 OpenExam/EXAMOS 项目代码和文档的审计发现，系统的总体架构和页面结构基本完整，但核心学习闭环存在明显缺口。一些设计理念（如能量感知计划、知识记忆引擎、跨学科语言学习）尚未完全落地。官方产品审计报告指出，多数 API 端点和前端页面按计划完成，但 Daily Review 忽略了能量数据，诊断结果和模拟考试结果没有反馈到记忆模型，导致学习循环中断【turn14file0†L4-L9】。此外，认知科学引擎有 7 个，但只有 4 个在 UI 中可用；无间隔可视化、扩张因子静态、不支持个性化【turn14file0†L11-L17】。语言学习模块 (LanguageOS) 提供了语料导入、分句、卡片生成等基础功能，但内容质量和词典支持不足，用户体验差。系统的 Resource Center 允许抓取外部网页，但没有严格的来源质量控制，造成内容噪声。
2 主要问题与影响分析
2.1 学习算法与闭环
问题
影响
现状引用
Daily Review 不使用 EnergyAwarePlanner
忽略学员精力状态，复习顺序不合理
审计报告指出“复习包不使用能量规划器”【turn14file0†L4-L9】
诊断结果及模拟成绩不反馈到记忆模型
知识状态无更新，难以根据错因调整间隔
同上【turn14file0†L4-L9】
静态间隔扩张因子 & 双套调度器并存
复习间隔无法个性化，CFA 卡片和错题卡使用不同调度器，造成记忆分裂【turn14file0†L11-L17】
￼
缺少遗忘曲线可视化
用户无法直观了解卡片到期时间和遗忘曲线
高价值缺口【turn14file0†L11-L17】
Copy table
2.2 内容与知识库
• Formula/概念库不足：目前每日复习仅从已有错题和少量公式生成“暖身内容”；无法覆盖用户笔记中的重要概念。
• 截图 AI 提取未实现：上传题目截图时仅保存图片，没有基于视觉模型的结构化抽取【turn14file0†L4-L9】。
• 无内置题库与公式检索：与 Anki、UWorld 等竞品相比，缺少官方题库、公式参考库及快速检索界面，这使系统很难单独应对考试。
• 资源抓取质量低：Resource Center 的爬虫未限制来源，常出现低质量文章，增加学习噪音。
2.3 LanguageOS 模块
问题
影响
说明
语料抓取与分句质量低
从网络获取的大量语料噪声高，造成词汇列表不精确；缺乏优选、去重与过滤机制。
Language Repository 仅按内容哈希去重，未对质量评分。
缺少词典数据
英语学习缺乏定义和同义关系；西语学习缺乏标准译法。
WordNet 等公开词汇数据库可提供大量语义关系
en.wikipedia.org
；GitHub 项目 spanish_data 提供了基于 Wiktionary/Tatoeba 构建的西英词典和频率表
github.com
。
语法分析与调度器有限
FSRS 调度算法已在语言模块中实现，但“grammar lens”和直觉图等功能通过特性开关默认关闭，未完全产品化。
￼
FSRS 与考试错题调度器分离
LanguageOS 卡片使用 FSRS6 调度器，而错题卡使用 SpacingScheduler，导致复习计划不统一。
￼
Copy table
2.4 体验与 UI
• 缺少间隔复习仪表盘：用户看不到即将到期的知识点，也无法了解掌握度。
• 新用户引导缺失：除语言模块外，CFA 驾驶舱缺乏引导流程，双语界面混杂，信息架构复杂。
• 移动端缺乏：与 Anki 等竞品相比，没有手机应用或 PWA 支持。
2.5 架构与代码质量
• 模块巨型化：workflows/core.py 文件超过 2600 行，包含 45 个函数，难以维护（审计报告 C3）。
• 事件格式不统一：Exam 与 Language 部分事件格式不同，缺乏统一版本；许多系统仍使用老的事件封装。
• 多套间隔系统并行：SpacingScheduler 与 FSRS6 并存，增加复杂性。
3 升级改进建议与实施计划
以下计划按问题分类提出改进策略，优先级综合考虑学习闭环完整性、内容质量提升、用户体验和生态扩展。
3.1 学习闭环与算法改进
1. 整合 EnergyAwarePlanner 到 Daily Review：在 daily_review_pack 中调用 EnergyAwarePlanner，根据当天能量评级调整复习顺序，避免高难度卡片在低精力时出现【turn14file0†L4-L9】。
2. 统一调度器（FSRS）：将错题卡 SpacingScheduler 替换为 FSRS6；允许基于稳定性、难度和回忆率预测下一次复习时间，并针对个人表现动态调整扩张因子。
3. 建立反馈链路：
• Diagnosis 产生的错因应写回 Knowledge Memory Engine，并调整难度与优先级；
• 模拟考试 retro 结果反哺调度器；
• 在 API 层添加事件触发器将这类反馈记录为事件，并在间隔调度中读取。
4. 遗忘曲线与复习仪表盘：实现一个可视化界面，展示每个卡片的当前状态、下次到期时间、知识熟练度曲线和历史评分。既可以利用 knowledge-readiness API，也可直接从 Memory Engine 提取数据。
5. 公式与概念练习增强：
• 构建公式数据库（基于 CFA 教材）、支持 formula_id → definition/derivation 映射，并在 Daily Review 中注入热身任务；
• 为每个公式建立练习卡片（理解记忆、推导、应用题），调度规则同 FSRS。
3.2 内容与知识库建设
1. PDF 笔记扫描管道：
• 利用 PyMuPDF4LLM 等工具直接解析 PDF 向量结构，无需 OCR，保留表格层次和布局
pymupdf.io
；该工具 CPU 即可运行，提取结构化文本仅需毫秒级
pymupdf.io
。
• 构建批处理脚本：依次读取用户上传的笔记 PDF，按页或章节分割为段落，调用 LLM 进行主题归类和知识点抽取，并生成 language.segment.created 或 knowledge_point.created 事件；对数学公式可使用 LaTeX 检测和 SymPy 解析，存储为公式库。
• 将抽取结果导入 local knowledge base，标注 source_ref 以便溯源，并在日常复习中调度。
2. 英语/西语词典导入：
• 集成 WordNet 数据库，创建 LanguageItem 对象：词条的 canonical_form 为单词，native_gloss 记录简明释义，同义词聚为 synset，建立 synonym、hyponym 等边，正如 WordNet 把词语组织为语义关系并提供定义与用法示例
en.wikipedia.org
。
• 从 spanish_data 项目提取西→英词典，其基于 Wiktionary 与 Tatoeba 数据构建，提供免费开放的西英词典和频率表
github.com
。将 es-en.data 中的西语词条和释义映射为 LanguageItem，在 native_gloss 字段填入英文解释。
• 实现词典导入接口 /api/language/imports：支持上传 CSV/JSON 字典，自动生成 language.source.imported 和 language.item.collected 事件；提示用户选择词性、标签等。
3. 资源中心质量提升：
• 在 resource_workflows.py 中增加来源白名单和垃圾内容过滤。例如限制 provider 在官方财经信息源（SEC EDGAR、FRED 等）和高质量语言学习网站；配置 resource-policy.yaml 的 fulltext_allowed_domains 来避免低质量域名。
• 针对 language lane 资源，引入 AI 质量评估模块：对文章计算难度、覆盖率、重复率，并标记 confidence 字段，低质量文档在索引时降权或拒绝。可使用轻量 LLM 生成标题摘要并判断是否含有“方案/广告”等噪音词。
• 开发订阅管理 UI，允许用户选择信任的 RSS 或 API 来源。
4. 题库与批量导入：制定题库模板（如 CSV 带有题干、选项、答案、解释、LOS），提供批量上传接口，并自动生成 MistakeCard；与资源中心结合，允许导入公开真题和练习题。
3.3 LanguageOS 完善与拓展
1. FSRS 调度与评分：语言模块已实现 FSRS6 调度器并支持 again/hard/good/easy 四级评分【turn8file0†L181-L259】。需在前端复习界面提供快捷键和评分按钮；完善 FSRSThreshold 调整功能（如算法个性化）。
2. 词汇类型和技能转移：按照规范文档中要求的项目类型（word、phrase、idiom、collocation 等）和卡片类型（recognition、production、cloze、dictation、shadowing 等）【turn7file0†L133-L144】，在 UI 中提供相应练习模式，并加入听力、口译、写作等 session 记录（session_type= listening/dictation/...）。
3. 语法分析与直觉图：
• 默认打开 language_grammar_lens 和 language_intuition_graph 特性；分析每个分句的词性和 CEFR 等级，允许用户编辑并生成 language.grammar.edited 事件【turn8file0†L268-L291】。
• 构建 intuiton graph 可视化，显示同义词、反义词、搭配、误译、错误朋友等边【turn7file0†L234-L247】；支持搜索和跳转。
4. 跨域桥接：实现 exam.language_gap.detected 事件的 UI 呈现。当 CFA 错题包含专业术语时，自动在 Language 模块中生成对应词条和卡片，帮助用户同时提升语言和专业知识。
5. 改善语料抓取：添加质量阈值和去重逻辑，防止大量低质量句子进入系统。考虑接入公共语料库（OpenSubtitles、News Crawl）并只保留高频、领域相关的句子。
6. 导出和同步：支持将语言卡片导出为 Anki 格式或 CSV【turn7file0†L332-L341】，方便用户在其他应用中学习；同时提供 Obsidian 插件同步功能，实现数据互通。
3.4 体验与界面优化
1. 统一语言与引导：根据审计报告，所有页面应采用统一语言（推荐中文或英文）；从 LanguageOS 移植 Guided Onboarding 流程至 CFA 驾驶舱，指导新用户如何记录错题、查看诊断报告、设置能量状态并进入复习。
2. 复习可视化与快捷操作：
• 在仪表盘展示待复习卡片的统计分布、知识熟练度及遗忘曲线；提供日历视图显示复习计划；
• 在复习界面添加键盘快捷键（数字 1–4 对应 again/hard/good/easy），提高评分效率。
3. 移动端与 PWA：实现响应式设计或发布 PWA，支持离线访问；利用浏览器 Service Worker 缓存 JSONL 事件和 Markdown 投影，保证无网络也可学习。
4. 信息架构重组：将 Resource Center 划分为多个选项卡（订阅、抓取、导入、库）；为公式库、错因分析、语言学习提供独立导航；采用层级侧栏而非单页长列表。
5. 可访问性与动效：遵循 WCAG 2.2 标准；对减弱动画偏好（prefers-reduced-motion）的用户禁用 GSAP 动效【turn7file0†L283-L317】；为每个控件提供 aria-label；颜色不是唯一状态指示。
3.5 架构和代码质量
1. 文件分割与模块化：将 workflows/core.py 按领域拆分为子模块：错题管理、复习包生成、能量规划、诊断反馈等；引入共用接口（例如 SchedulerProtocol）以便替换调度算法。
2. 统一事件封装：升级所有事件为 EventEnvelopeV2；复用 LanguageRepository.append 中 idempotency_key 设计，实现幂等写入【turn12file0†L33-L55】。
3. 单一调度器：将 SpacingScheduler 与 FSRS6 合并，卡片保存 fsrs_state 字段，节省状态同步开销；在 Python 包和 TS 客户端中提供公共数据结构。
4. 重写资源抓取流程：完善 resource-policy.yaml 配置；增加数据质量审核机制；在爬虫中添加异常捕获和路径遍历防护（修复审计报告 P0 故障）。
5. 测试覆盖与自动化：针对新的学习闭环添加单元测试和 E2E 测试，确保新特性在 CI 中稳定运行；配置 GitHub Actions 自动运行 Python/Ts tests、lint、type check 和 accessibility 检查。
4 对标其他系统的启示
竞品
优势
需借鉴的细节
Anki
成熟的间隔复习算法 (FSRS-5)、丰富卡片类型、移动端同步社区卡片
引入 FSRS 个性化调度及多卡片类型；提供手机/PWA 同步；允许导入/导出社区卡组
UWorld/Schweser
庞大题库、详细解释、模拟考试与评分
建设题库导入模板；提供全真模考功能与即时评分；对错题提供高质量解释
Quizlet/Duolingo
简洁 UI、游戏化体验、多语言课程
改善界面友好度；引入游戏化元素；设计完整语言课程 (listening、speaking、grammar)
Copy table
5 结论
OpenExam/EXAMOS 已建立了完整的技术框架，但要达到“前沿的学习系统”的目标，还需从闭环完成、内容升级、语言学习、用户体验、代码架构等多个维度进行优化。通过引入高质量词典数据和 PDF 笔记扫描、统一 FSRS 调度、加强反馈和可视化、提升资源质量以及改善 UI/UX，可使系统更科学、更高效、更易用。
Share
。欢迎查看详细的分析与建议，便于有针对性地提升系统的功能完善度与生态可持续性。如需对某一部分做进一步探讨，请随时告知。



OpenExam 极致细节补强计划：KnowledgeOS + DailyReview 2.0 + Dictionary-driven LanguageOS
0. 总体原则：不要再扩功能，先把“核心闭环”做深

你的系统下一阶段不应该继续横向堆页面，而应该做三个深内核：

KnowledgeOS：保证重要知识点、公式、易错边界不漏
DailyReview 2.0：保证每天复习真的主动回忆、评分、更新记忆状态
Dictionary-driven LanguageOS：保证语言学习以高质量词典 + 真实语境为核心

现在 ResourceOS 已经有合法抓取、license、hash、private FTS5 index 的基础，README 也说明未知版权默认 metadata + short excerpt，licensed full text 存在 .system/private/resources/ 并排除 Git。 这很好，后面要做的是：从“能抓内容”升级到“能判断内容是否值得进入复习系统”。

1. DailyReview 的根问题：知识点和公式来源太窄

你现在 daily review 的“知识点和公式”主要从 MOC 和模块 Markdown 中抽取。extract_moc_atoms() 只读对应 subject 的 MOC，并且主要抓 ## 2. 下面的表格行；extract_expanded_module_atoms() 也依赖标题里包含 classifier / formula / decision / trap / recall 等关键词。 这意味着：只要你的笔记格式不是这个结构，重要知识就可能完全进不了 daily review。

虽然 daily review 已经会把 due knowledge points 注入 warm start，但找不到公式/decision 时会退化成记忆状态或 decay risk，而不是真正的知识解释。 所以你担心“复习时忘掉很多重要知识点”是合理的。

1.1 新增 Knowledge Source Pipeline：扫描你的笔记 PDF 补本地知识库

目标：你上传一份 CFA/FRM/金融/语言学习笔记 PDF，系统自动把里面的知识点、公式、例题、易错边界抽成结构化知识资产，而不是只当成普通文档。

新增模块：

packages/knowledge-ingestion/
  src/knowledge_ingestion/
    pdf_loader.py
    layout_parser.py
    formula_extractor.py
    table_extractor.py
    note_segmenter.py
    atom_extractor.py
    atom_classifier.py
    los_mapper.py
    coverage_auditor.py
    enrichment_planner.py

.system/app/
  knowledge_source_models.py
  knowledge_source_workflows.py
  knowledge_coverage.py
  knowledge_promotion.py

apps/api/routers/
  knowledge_sources.py
  knowledge_coverage.py

apps/web/src/app/knowledge/
  sources/page.tsx
  coverage/page.tsx
  review-lab/page.tsx

新增数据模型：

class KnowledgeSource:
    source_id: str
    source_type: Literal["pdf", "markdown", "web", "manual", "ai_enriched"]
    title: str
    file_hash: str
    license_mode: str
    imported_at: str
    subject_hint: str
    profile_id: str

class KnowledgeSpan:
    span_id: str
    source_id: str
    page: int | None
    locator: str
    text: str
    bbox: dict | None
    confidence: float

class KnowledgeAtom:
    atom_id: str
    atom_type: Literal[
        "definition",
        "formula",
        "formula_variant",
        "procedure",
        "example",
        "exam_trap",
        "comparison",
        "condition",
        "exception",
        "mnemonic"
    ]
    subject: str
    los: str
    heading: str
    trigger: str
    answer: str
    formula_latex: str
    example: str
    boundary: str
    source_span_ids: list[str]
    confidence: float
    promotion_status: Literal["draft", "confirmed", "rejected", "superseded"]

PDF 解析流程：

PDF upload
→ save original file under .system/private/knowledge-sources/
→ SHA-256 hash
→ page text extraction
→ table/formula extraction
→ semantic segmentation
→ candidate KnowledgeAtom extraction
→ LOS/topic mapping
→ duplicate detection against MOC atoms
→ quality scoring
→ quarantine if low confidence
→ user confirm
→ promote to local KnowledgeOS
→ daily review uses it

PDF 不能只靠普通文本抽取，特别是金融公式、表格、符号、页眉页脚很容易乱。学术 PDF 转结构化文本本身就是难题，Nougat 这类研究也指出 PDF 丢失了语义信息，尤其是数学表达式，所以更合理的做法是“抽取 + 置信度 + quarantine + 人工确认”，而不是自动相信解析结果。

1.2 Knowledge Coverage Audit：让系统告诉你“你漏了什么”

DailyReview 必须从“根据已有错题复习”升级为“根据考试知识图谱查漏补缺”。

新增：

Knowledge Coverage Dashboard

显示：

Subject coverage
Module coverage
LOS coverage
Formula coverage
Definition coverage
Trap coverage
Example coverage
Review coverage
Decay risk
Unlinked mistakes
Unlinked PDF notes
Uncovered high-weight LOS

Coverage 计算：

coverage_score =
  0.25 * los_atom_coverage
+ 0.20 * formula_coverage
+ 0.20 * reviewed_recently_ratio
+ 0.15 * mistake_linkage_ratio
+ 0.10 * example_coverage
+ 0.10 * exam_trap_coverage

如果某个 LOS 有错题，但没有对应知识点/公式/例题，则生成：

knowledge_gap_detected

如果某个 PDF 中有公式，但 MOC 和 daily review 从未出现过，则生成：

orphan_knowledge_atom

如果某个重要公式长期未复习，则生成：

decay_risk_critical
2. DailyReview 2.0：从 Markdown 变成主动回忆实验室

你的审计文档指出 daily review 现在仍偏 raw Markdown，没有 recall-first / reveal-later、没有 review quality scoring、没有 link back to diagnosis。 代码中 render_review_pack() 虽然已经把 RetrievalEngine、SelfExplanationPrompt、WorkedExampleFader 写进 Markdown，但用户仍然不是在交互式训练，而是在读材料。

2.1 新 DailyReview 页面结构

新页面 /review/lab：

1. Knowledge Recall
2. Formula Lab
3. Mistake Cards
4. Worked Example Fading
5. Interleaving Drill
6. Transfer Drill
7. Post-review Scoring

每个知识点都必须遵守：

先回忆 → 再揭示 → 再评分 → 再更新记忆状态

不要一上来展示答案。

2.2 DailyReview item 的最小结构

现在 warm start item 只是 trigger / formula / decision / source。未来必须升级为：

class DailyReviewUnit:
    unit_id: str
    unit_type: Literal["knowledge", "formula", "mistake", "trap", "example", "transfer"]
    prompt: str
    recall_instruction: str
    answer: str
    formula_latex: str
    worked_example: str
    common_wrong_path: str
    exam_trap: str
    source_refs: list[str]
    source_spans: list[str]
    due_reason: str
    memory_state: str
    priority: int
    interaction_mode: Literal["recall", "derive", "solve", "compare", "explain"]
2.3 每个 item 必须有独立结果，而不是整包完成

现在 complete_daily_review() 对 knowledge point 批量写 outcome=reviewed，confidence_after=2。 这会造成一个严重问题：用户只是点了完成，系统就默认所有知识点都复习成功了。

必须改成：

review_unit.completed

每个 unit 保存：

{
  "unit_id": "...",
  "outcome": "recalled | partial | forgot | skipped",
  "confidence_before": 0-4,
  "confidence_after": 0-4,
  "time_spent_seconds": 45,
  "needed_hint": true,
  "answer_quality": 0.0-1.0,
  "fix_rule_helpful": true,
  "next_action": "shorten_interval | keep | expand | add_drill"
}
2.4 Formula Lab

新增 /review/formula-lab，专门解决你对公式部分不满意的问题。

每个公式卡必须包括：

公式名称
公式本体 LaTeX
变量解释
适用条件
不适用条件
常见变体
计算器按键 / BA II Plus steps
例题
易错点
相似公式区分
来源 PDF/MOC span

这对 CFA 特别重要。你之前也一直要求 CFA 题讲解要有 BA II Plus 计算器步骤，所以 Formula Lab 应该内置：

class FormulaAtom:
    formula_id: str
    formula_name: str
    latex: str
    variables: list[dict]
    applies_when: list[str]
    not_when: list[str]
    variants: list[str]
    ba_ii_plus_steps: list[str]
    example_refs: list[str]
    confusion_refs: list[str]
3. 联网补全：不是乱爬，而是 Evidence Enrichment

你现在爬取内容质量低，本质原因通常不是爬虫差，而是缺少 source quality scoring + promotion gate。

已有 ResourceOS 的 ResourceDocument 有 lane、provider、url、title、content_hash、license_mode、content_ref、excerpt、language、topic、metadata 等字段。 私有索引也已经有 raw/manifests/audits 和 FTS5。 现在要加的是“资源是否值得进学习系统”。

3.1 Resource Quality Score

新增：

class ResourceQualityScore:
    document_id: str
    authority_score: float
    level_fit_score: float
    domain_relevance_score: float
    extractability_score: float
    license_safety_score: float
    signal_density_score: float
    duplicate_penalty: float
    spam_penalty: float
    final_score: float
    recommendation: Literal["promote", "candidate", "metadata_only", "reject"]

评分公式：

final_score =
  0.20 * authority_score
+ 0.20 * domain_relevance_score
+ 0.15 * level_fit_score
+ 0.15 * signal_density_score
+ 0.15 * extractability_score
+ 0.10 * license_safety_score
- 0.05 * duplicate_penalty
- 0.10 * spam_penalty

不要把所有抓到的内容都变成学习材料。应该先进入：

Resource Candidate Queue

用户确认后再进入：

Knowledge Enrichment / Language Corpus
3.2 联网补知识的正确顺序

对 CFA/知识点：

1. 本地 MOC
2. 用户上传 PDF 笔记
3. 用户上传教材/讲义摘录
4. ResourceOS 已保存的本地私有资料
5. 公开合法网页/官方材料/公开报告
6. AI 只做抽取、总结、对齐，不作为事实源

AI 不应该直接“编一个更好的知识点”。它应该输出：

候选解释
候选公式表
候选例题
候选易错边界
每条对应 source_span
置信度
是否需要人工确认
4. LanguageOS 根本重做：Dictionary-driven，不是 random corpus-driven

你现在 LanguageOS 低质量的原因，在代码里很明显。

LanguageProfile 只有 level_target，没有 current level、target level、domain priority、词汇目标、词典优先级。 CardFactory 的卡片也过于浅：recognition 直接 prompt=form，back answer=form；production 用 gloss 或 Translate；dictation 只是 “Type what you hear: 'form'”，并没有真正音频、释义、词义辨析、搭配、例句、语域。 前端 review 页也只是取第一张 due card、翻面、打 again/hard/good/easy，再从第一个 corpus item 生成卡片。

所以结论很明确：LanguageOS 现在只是 demo，不是可用的语言学习系统。

4.1 DictionaryOS：上传英英词典 + 西英词典作为语言内核

你提出上传英译英词典、西译英词典，这个方向非常正确。语言系统不应该先依赖爬虫语料，而应该先有 权威词典内核。

新增：

.system/private/language-dictionaries/
  english_english/
  spanish_english/
  manifests/
  indexes/

新增模型：

class DictionarySource:
    dictionary_id: str
    language_pair: str          # en-en, es-en
    title: str
    format: str                 # json, csv, tsv, stardict, mdict, tei_xml, pdf
    file_hash: str
    license_mode: str
    imported_at: str
    priority: int               # user dictionary > open dictionary > inferred

class LexicalEntry:
    entry_id: str
    dictionary_id: str
    lemma: str
    language: str
    pos: str
    ipa: str
    audio_ref: str
    frequency_band: str
    cefr_level: str
    domain_tags: list[str]

class Sense:
    sense_id: str
    entry_id: str
    sense_number: int
    definition_en: str
    translation_zh: str
    translation_en: str
    examples: list[str]
    collocations: list[str]
    register: str
    domain: str
    synonyms: list[str]
    antonyms: list[str]
    usage_notes: str

class BilingualMapping:
    mapping_id: str
    source_lemma: str       # Spanish
    target_lemma: str       # English
    source_sense_id: str
    target_sense_id: str
    confidence: float
    false_friend_warning: str
4.2 推荐使用的开放词典基底

你上传的词典作为最高优先级；系统也可以内置开放词典作为 fallback：

WordNet 是大型英语词汇数据库，名词、动词、形容词、副词被组织成 synsets，并有定义、例句、语义/词汇关系，适合作为英英词义网络底座。
FreeDict 目标是提供真正自由的双语词典，包含 140+ 词典和 45+ 语言，并使用 TEI XML，适合作为双语词典导入格式参考。
Wikidata Lexemes 支持 Lexeme、Form、Sense，并有 grammatical gender、conjugation、translation、synonym、antonym、false friend、usage example、IPA/pronunciation audio 等属性，特别适合西语形态、false friends、跨语言关系。
4.3 词典导入流程
Upload dictionary
→ detect format
→ parse entries
→ normalize lemma
→ split senses
→ extract definitions/examples/collocations
→ build lexical index
→ build FTS5 search
→ build lemma lookup
→ build sense graph
→ generate candidate cards
→ user confirms deck policy

支持格式：

CSV / TSV
JSON
TEI XML
StarDict
MDict / MDX, optional converter
PDF fallback, low confidence
EPUB
plain text

PDF 词典可以支持，但必须低置信度，因为 PDF 词典结构复杂。最优先应该是结构化词典。

4.4 Language Card 2.0：禁止 front=answer 的低质量卡

新增规则：

production / recognition / cloze / dictation / grammar / collocation 卡片中，
front 不允许等于 back answer。

现在 CardFactory 的 recognition 基本就是 front=form, answer=form，这对学习几乎无效。

新卡片类型：

1. Definition → Word
2. Word → Sense Discrimination
3. Example Cloze
4. Collocation Completion
5. Reverse Translation
6. Audio Dictation
7. Shadowing
8. Spanish Gender
9. Spanish Conjugation
10. False Friend Warning
11. Register Choice
12. Finance/Academic/CS Domain Usage

例子：

Front:
Definition: to reduce the value of existing shares by issuing more shares.
Context: The new share issue may ___ existing shareholders.

Back:
dilute / dilutive
Sense: finance
Collocations: dilutive effect, diluted EPS, anti-dilutive
CFA link: EPS / share issuance
Source: dictionary + CFA segment

西语例子：

Front:
Choose the right meaning:
actualmente

Back:
currently / at present
Not: actually
False friend warning: English "actually" = en realidad
4.5 CEFR 和领域不是装饰，而是过滤器

CEFR 官方框架被广泛用于课程、教材、考试和评估设计，Companion Volume 还扩展到 mediation、online interaction、plurilingual/pluricultural competence 等描述。 所以 LanguageOS 应该把 CEFR 做成硬过滤：

current_level: A1-C2
target_level: A1-C2
allowed_new_item_band:
  A1: A1 + limited A2
  A2: A1-A2 + limited B1
  B1: A2-B1 + limited B2
  B2: B1-B2 + selected C1
  C1: B2-C1 + selected C2
  C2: all + nuance/register

不要让 A1 用户看到一堆 C1 金融表达，也不要让 B2-C1 用户背低频但无意义的日常词。

5. Language Resource 质量重建

你说“爬取内容质量极低”，所以要把 ResourceOS 改成 “curated + ranked + user-confirmed”。

5.1 内容进入 LanguageOS 的四级门
Level 0: discovered metadata
Level 1: candidate resource
Level 2: imported corpus
Level 3: promoted learning material
Level 4: reviewed and validated item

任何网页/视频/文章不能直接从 Level 0 进入卡片。

5.2 Resource Candidate 必须显示为什么推荐

每条资源显示：

为什么适合你
CEFR 匹配度
领域匹配度
是否有字幕/文本
预计可产出多少高质量词条
版权状态
是否适合听力/阅读/写作
是否已有重复内容
5.3 高质量资源策略
英语金融：SEC 文件、FRED/宏观材料、公司报告、投资者信、CFA 用户笔记
英语学术：arXiv abstract、open-access paper、课程讲义、用户 PDF
计算机科学：官方文档、开源 README、StackExchange API 摘要、论文 abstract
商务英语：年报、商业案例、邮件模板、会议表达
日常英语：有字幕的视频、播客 transcript、分级读物、用户收藏文章
西语：分级材料、字幕、新闻简版、对话文本、用户词典、双语例句

关键：互联网内容只负责提供真实语境，词义权威来自词典，学习优先级来自用户目标和复习数据。

6. 生态细节问题清单
6.1 KnowledgeOS 问题
问题	当前表现	解决
公式覆盖不稳定	依赖 MOC ## 2. 和表格格式	PDF/Markdown atom extractor + coverage audit
重要知识漏复习	daily review 只看错题、MOC、overlay	syllabus coverage + decay risk + orphan notes
复习结果太粗	complete daily review 默认所有 knowledge reviewed	每个 unit 单独评分
公式没有 UI	audit 已指出 no formula reference library UI	Formula Lab
AI 没有证据约束	容易生成看似合理内容	每条 AI enrichment 必须有 source span
6.2 LanguageOS 问题
问题	当前表现	解决
词卡低质量	front/back 太简单	Dictionary-driven card factory
没有真正词典内核	只有 native_gloss 字段	DictionaryOS + LexicalEntry/Sense
内容质量低	资源进入 corpus 缺少质量门	ResourceQualityScore + Candidate Queue
CEFR 不够用	只有 level_target	current/target/allowed band
西语学习太浅	没有系统处理性数格、变位、false friends	Spanish Morphology Engine
统计太浅	stats 只有 count	mastery、retention、output gap、domain progress

当前 language_stats() 只返回 source_count、segment_count、item_count、card_count、due_count 等数量，不足以评估学习质量。

7. 分阶段 Codex 实施计划
Wave 1：Knowledge Source PDF Ingestion

目标：上传你的笔记 PDF，生成可确认的知识点/公式/例题/陷阱。

文件：

packages/knowledge-ingestion/src/knowledge_ingestion/*
.system/app/knowledge_source_models.py
.system/app/knowledge_source_workflows.py
apps/api/routers/knowledge_sources.py
apps/web/src/app/knowledge/sources/page.tsx
.system/tests/test_knowledge_pdf_ingestion.py

验收：

1. 上传 PDF 后保存原始文件和 hash
2. 能按页生成 KnowledgeSpan
3. 能抽取 definition/formula/example/trap candidates
4. 每个 atom 有 source_span_ids
5. 低置信结果进入 quarantine
6. 用户确认后写入 knowledge.atom.promoted
Wave 2：Knowledge Coverage Audit

目标：告诉你哪些重要知识没进 daily review。

文件：

.system/app/knowledge_coverage.py
apps/api/routers/knowledge_coverage.py
apps/web/src/app/knowledge/coverage/page.tsx
.system/tests/test_knowledge_coverage.py

验收：

1. 显示 subject/module/LOS/formula coverage
2. 显示 orphan mistakes
3. 显示 orphan PDF atoms
4. 显示 overdue knowledge atoms
5. 可以一键加入 DailyReview queue
Wave 3：DailyReview Lab

目标：从 Markdown review 变成交互式 recall-first 复习。

文件：

.system/app/review_lab_models.py
.system/app/review_lab_workflows.py
apps/api/routers/review_lab.py
apps/web/src/app/review/lab/page.tsx
apps/web/src/components/review/RecallRevealCard.tsx
apps/web/src/components/review/FormulaLabCard.tsx
apps/web/src/components/review/ReviewOutcomeButtons.tsx

验收：

1. 每个知识点先遮答案
2. 用户必须选择 recalled/partial/forgot/skipped
3. 每个 unit 独立更新 KnowledgeMemoryEngine
4. 支持公式推导卡
5. 支持 worked-example fading
6. 支持 self-explanation 输入框
7. 复习完成后生成 review quality report
Wave 4：DictionaryOS

目标：上传英英词典和西英词典，建立语言学习权威内核。

文件：

packages/language-science/src/language_science/dictionary/*
.system/app/dictionary_models.py
.system/app/dictionary_workflows.py
apps/api/routers/language_dictionaries.py
apps/web/src/app/language/dictionaries/page.tsx
.system/tests/test_dictionary_import.py

验收：

1. 支持上传 en-en / es-en 词典
2. 支持 CSV/TSV/JSON/TEI XML
3. 每个 entry 有 senses/examples/collocations
4. 词典数据进入 private dictionary index
5. 不进入 Git
6. 可搜索 lemma / definition / translation
Wave 5：Language Card Factory 2.0

目标：彻底淘汰低质量词卡。

文件：

packages/language-science/src/language_science/cards_v2.py
packages/language-science/src/language_science/spanish_morphology.py
packages/language-science/src/language_science/lexical_graph.py
apps/web/src/app/language/review/page.tsx
apps/web/src/components/language/LanguageReviewDeckV2.tsx
.system/tests/test_language_cards_v2.py

验收：

1. 生成 Definition → Word
2. 生成 Word → Sense
3. 生成 Collocation Cloze
4. 生成 Spanish Gender / Conjugation
5. 生成 False Friend 卡
6. 生成 Finance/Academic/CS domain usage 卡
7. 测试保证 front != answer
Wave 6：Resource Quality Engine

目标：解决“爬取内容质量极低”。

文件：

packages/resource-ingestion/src/resource_ingestion/quality.py
packages/resource-ingestion/src/resource_ingestion/candidate_queue.py
.system/app/resource_promotion.py
apps/api/routers/resource_candidates.py
apps/web/src/app/resources/candidates/page.tsx
.system/tests/test_resource_quality.py

验收：

1. 每条 resource candidate 有 quality score
2. 低质量内容不能自动进入 corpus
3. 用户可以 approve/reject
4. 系统解释推荐理由
5. promotion 事件可追踪
Wave 7：Unified Learning Dashboard

目标：让用户看到“我是不是变强了”。

新增指标：

Knowledge coverage
Formula mastery
Review quality
Forgotten important atoms
Language retention
Dictionary-backed vocabulary count
Output gap
Listening/dictation accuracy
Spanish morphology accuracy
Resource quality distribution
8. Feature Flags
knowledge_pdf_ingestion: false
knowledge_atom_extraction: false
knowledge_coverage_audit: false
daily_review_lab: false
daily_review_unit_scoring: false
formula_lab: false
dictionary_os: false
dictionary_private_index: false
language_cards_v2: false
spanish_morphology_engine: false
resource_quality_gate: false
resource_candidate_queue: false
ai_knowledge_enrichment: false

回滚原则：

1. 所有新增内容都是事件和 projection，不改旧历史
2. DailyReview Lab 可关闭，退回旧 Markdown daily-review
3. DictionaryOS 可关闭，不影响已有 LanguageOS events
4. PDF atom extraction 可关闭，保留原 PDF source
5. Resource quality gate 可只读，不影响 ResourceOS 基础抓取
9. 给 Codex 的总提示词
You are upgrading OpenExam from a structurally strong but detail-incomplete learning OS into a production-grade KnowledgeOS + DailyReview 2.0 + Dictionary-driven LanguageOS.

Do not rewrite the whole architecture. Reuse:
- .system event streams
- local-first private storage
- ResourceOS
- KnowledgeMemoryEngine
- FastAPI routers
- Next.js cockpit
- Obsidian projections

Core problems to fix:
1. DailyReview knowledge/formula section is too narrow because it depends on MOC/table heuristics.
2. User needs to upload note PDFs to fill local knowledge base.
3. DailyReview must become recall-first and item-scored, not raw Markdown reading.
4. LanguageOS cards are too shallow and not suitable for real vocabulary learning.
5. Resource crawling quality is too low and needs a candidate quality gate.
6. User wants to upload English-English and Spanish-English dictionaries as the lexical source of truth.

Implement in waves:
Wave 1: Knowledge PDF ingestion.
Wave 2: Knowledge coverage audit.
Wave 3: DailyReview Lab with recall/reveal/scoring.
Wave 4: DictionaryOS for en-en and es-en dictionaries.
Wave 5: Language Card Factory v2.
Wave 6: Resource Quality Engine.
Wave 7: Unified dashboard.

Acceptance principles:
- Every knowledge atom has source spans.
- Every formula has variables, conditions, example, trap, and source.
- Every daily review unit records outcome individually.
- No card should have front equal to answer.
- Dictionary entries must support senses, examples, collocations, synonyms, false friends, morphology.
- Internet resources must pass quality and license gates before promotion.
- AI is optional and can only create candidates with citations/source spans.
- All new features are behind feature flags.
- All projections are rebuildable.
最重要的战略判断

你现在不缺“宏大设计”，你缺的是 学习对象的质量控制。

未来 OpenExam 的强大不在于：

抓更多网页
生成更多卡片
做更多 dashboard

而在于：

每个知识点是否来源清楚
每个公式是否完整
每个复习项是否真的被主动回忆
每个词是否有权威释义和真实语境
每个资源是否值得进入学习系统
每次复习是否真实改变下一次安排

所以我建议你下一步把 Codex 的主要任务集中在一句话：

把 OpenExam 从“能生成学习材料”升级为“能筛选、验证、追踪、复习高价值学习资产”的系统。

做到这一点，你的 DailyReview 才不会让重要知识漏掉，LanguageOS 才会真正能学单词和语言，整个系统也才会从“设想很好”变成“每天真的提高效率”。