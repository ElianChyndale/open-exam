# Local evals

`cases.jsonl` contains representative local workflow checks for the mistake system.

`run_local.py` executes the workflow layer directly so the file-based system can be verified without a live OpenAI API call.

