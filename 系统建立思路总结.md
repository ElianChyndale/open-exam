---
title: 系统建立思路总结
tags:
  - CFA
  - OpenExam
  - 系统设计
status: draft
---

# 系统建立思路总结

> 这份总结只回答三件事：系统是什么，为什么这么建，怎么运转。

## 01 定位

**OpenExam / CFA Level I 本地 Agent 学习操作系统**

核心目标不是“多记一点”，而是把每一次做题、卡壳、复盘、误判和 agent 失误，转成下一次更快、更准、更稳的行动规则。

### 系统要解决的事

- 更快定位弱点
- 更高质量复盘
- 更少重复犯错
- 更强 agent 协作

### 系统边界

- 不做普通题库
- 不做课程平台
- 不让 Obsidian 充当主存储
- 不让 AI 脱离证据直接给结论

---

## 02 四层架构

| 层级 | 作用 | 主目录 |
|:---|:---|:---|
| Capture Layer | 捕获原始事件 | `.system/events/` |
| Memory Layer | 沉淀长期资产 | `.system/memory/` |
| Decision Layer | 生成策略与复盘结论 | `.system/app/` + `skills/` |
| Projection Layer | 导出为可阅读页面 | `CFA_tier1/` |

### 数据流

`用户动作` -> `事件层` -> `memory / workflow` -> `dashboard / Obsidian 投影`

### Source of Truth 顺序

1. `.system/events/` 和 `.system/events/catalog.sqlite3`
2. `.system/memory/`
3. `.system/app/` 的工作流逻辑
4. `skills/` 的行为规范
5. `CFA_tier1/` 的投影页面
6. 人工摘要和临时聊天结论

---

## 03 建系统的核心思路

### 先做证据闭环，不先做漂亮页面

系统的基本链路是：

`题目 / 偏差 / agent 失误` -> `结构化事件` -> `错题卡 / 复盘卡` -> `pattern` -> `strategy` -> `validation`

### 错误必须分层

| 类型 | 含义 |
|:---|:---|
| `question` | 题目层错误 |
| `bias` | 学习过程偏差 |
| `agent` | agent 执行失误 |

如果一开始分不清，就先按 `question` 记录，再在复盘里升级为 `bias` 或 `agent`。

### AI 不是直接输出答案，而是参与治理

agent 的工作顺序应该是：

- 先校验，再输出
- 先看原始事件，再给结论
- 先补证据，再做总结
- 能追责、能回放、能重建

### Obsidian 只是展示层

`CFA_tier1/` 是 projection，不是 source of truth。  
真正的学习资产在 `.system/events/` 和 `.system/memory/`。

---

## 04 运行节奏与治理规则

### 五层运行节奏

- **即时层**：题错就 `record-mistake`，卡住就 `review-session`，agent 错就 `audit-agent`
- **单次会话层**：总结今天错在知识、过程还是 agent
- **Mock 层**：`pre-mock-brief` / `post-mock-retro`
- **周度层**：`mine-patterns`
- **月度层**：经验治理、策略去重、validation 淘汰

### 标准 CLI 是系统主入口

```powershell
python scripts/cfa.py record-mistake --payload "{...}"
python scripts/cfa.py review-session --payload "{...}"
python scripts/cfa.py audit-agent --payload "{...}"
python scripts/cfa.py mine-patterns
python scripts/cfa.py daily-review-pack --focus-topic "Corporate Issuers"
python scripts/cfa.py write-todo --payload "{...}"
python scripts/cfa.py pre-mock-brief
python scripts/cfa.py post-mock-retro --session-id mock-2
```

### 经验治理原则

- 单次错题可以生成
- 偏差记录可以生成
- agent 失误可以生成
- 高复现 pattern 才值得长期保留
- 能改变下一次决策的 strategy 才值得保留
- 需要验证的 validation 才应该进入治理层

### 质量门槛

- 结论必须能回到原始事件
- 必须可执行，不只是抽象建议
- 规则性结论要能验证
- 真正影响下一次决策，才值得长期保留

---

## 05 当前升级路线

### 不是推倒重来，而是分波次升级

- 保留健康内核
- 分波次推进
- 每一波都必须能回到事件和投影
- 不能让未来规划冒充当前完成度

### 当前方法

1. 先稳核心闭环
2. 再扩展能力
3. 再补知识覆盖、mock 反馈、能量感知、知识图谱
4. 再推进更强 UI、agent brief、MCP、信任适配器

### 当前最重要的原则

- 不让系统只长展示，不长证据
- 不让新功能没有回放能力
- 不让 workflow 失去一致性
- 不让 Obsidian 反客为主

---

## 06 一句话总结

**这套系统的本质，是把 CFA 备考从“记内容”改造成“管理证据、修正决策、持续升级学习策略”的本地操作系统。**

---

## 参考文档

- [AGENTS.md](./AGENTS.md)
- [PLAN.md](./PLAN.md)
- [ARCHITECTURE.md](./ARCHITECTURE.md)
- [README.md](./README.md)
- [docs/OPENEXAM_RECOVERY_EXECUTION_PLAN.md](./docs/OPENEXAM_RECOVERY_EXECUTION_PLAN.md)
- [docs/research/2026-06-01-upgrade-plan.md](./docs/research/2026-06-01-upgrade-plan.md)
- [docs/research/2026-06-01-audit-report.md](./docs/research/2026-06-01-audit-report.md)
- [docs/research/product-audit-report.md](./docs/research/product-audit-report.md)
- [docs/cfa-knowledge-base-display-evolution.md](./docs/cfa-knowledge-base-display-evolution.md)
