[project]
name = "exam-core"
version = "0.1.0"
description = "Abstract exam engine — CFA, FRM, CPA, Bar Exam domain models"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
