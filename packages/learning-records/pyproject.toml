[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "learning-records"
version = "0.1.0"
description = "Canonical event envelopes for OpenExam learning records."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
