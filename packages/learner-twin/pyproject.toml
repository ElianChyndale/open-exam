[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "learner-twin"
version = "0.1.0"
description = "Deterministic sparse-data learner model for OpenExam."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
